{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "meta_gen",
  "type": "object",
  "properties": {
    "outputFile": {
      "type": "string",
      "default": "Meta.ts",
      "description": "The output file to write the generated .ts file to. Defaults to Meta.ts."
    },
    "syncVersionFromTag": {
      "type": "boolean",
      "default": false,
      "description": "Whether to sync the manifest version from the latest git tag. The tag must follow semver format (v1.0.0 or 1.0.0). When enabled, both BP and RP manifest.json files will be updated with the tag version. Defaults to false."
    }
  }
}
